# personal_finance

This independently runnable dbt project owns the personal-finance warehouse
graph. It remains on BigQuery and has no dependency on the wremotely project.

Use `../README.md` for runtime, profile, authentication, and validation setup.
Do not place credentials, local profiles, logs, or compiled artifacts here.
