# wremotely

This independently runnable dbt project owns the wremotely warehouse and
serving graph. It initially remains on BigQuery and has no dependency on the
personal-finance project.

Use `../README.md` for runtime, profile, authentication, and validation setup.
Do not place credentials, local profiles, logs, or compiled artifacts here.
