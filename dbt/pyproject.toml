[project]
name = "data-platform-dbt"
version = "0.1.0"
description = "dbt project for the data platform warehouse"
requires-python = ">=3.12,<3.13"
dependencies = [
    "dbt-bigquery>=1.11.1,<2",
]

[tool.uv]
package = false
