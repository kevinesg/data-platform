services:
  airflow-init:
    build:
      context: .
      dockerfile: Dockerfile
    image: ${DATA_PLATFORM_AIRFLOW_IMAGE:-data-platform-airflow:dev}
    volumes:
      - ./dags:/opt/airflow/dags
      - airflow-auth:/opt/airflow/auth
      - airflow-logs:/opt/airflow/logs
      - /var/run/docker.sock:/var/run/docker.sock

  api-server:
    build:
      context: .
      dockerfile: Dockerfile
    image: ${DATA_PLATFORM_AIRFLOW_IMAGE:-data-platform-airflow:dev}
    volumes:
      - ./dags:/opt/airflow/dags
      - airflow-auth:/opt/airflow/auth
      - airflow-logs:/opt/airflow/logs
      - /var/run/docker.sock:/var/run/docker.sock

  dag-processor:
    build:
      context: .
      dockerfile: Dockerfile
    image: ${DATA_PLATFORM_AIRFLOW_IMAGE:-data-platform-airflow:dev}
    volumes:
      - ./dags:/opt/airflow/dags
      - airflow-auth:/opt/airflow/auth
      - airflow-logs:/opt/airflow/logs
      - /var/run/docker.sock:/var/run/docker.sock

  scheduler:
    build:
      context: .
      dockerfile: Dockerfile
    image: ${DATA_PLATFORM_AIRFLOW_IMAGE:-data-platform-airflow:dev}
    volumes:
      - ./dags:/opt/airflow/dags
      - airflow-auth:/opt/airflow/auth
      - airflow-logs:/opt/airflow/logs
      - /var/run/docker.sock:/var/run/docker.sock

  triggerer:
    build:
      context: .
      dockerfile: Dockerfile
    image: ${DATA_PLATFORM_AIRFLOW_IMAGE:-data-platform-airflow:dev}
    volumes:
      - ./dags:/opt/airflow/dags
      - airflow-auth:/opt/airflow/auth
      - airflow-logs:/opt/airflow/logs
      - /var/run/docker.sock:/var/run/docker.sock
